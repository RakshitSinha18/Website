project_id = "ipuwwhksolvkswsnseis"

[functions.notify]
verify_jwt = false

# Providers (Stripe/Razorpay) call this without a Supabase JWT; it authenticates
# requests itself via webhook signature verification.
[functions.payment-webhook]
verify_jwt = false

# pg_cron calls this without a JWT; it authenticates via X-Reminders-Secret.
[functions.reminders]
verify_jwt = false
